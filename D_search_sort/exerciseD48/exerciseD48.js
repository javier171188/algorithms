'use strict';

const S = 'avsfvasbdackldabcd';
const T = 'abcd';

const subString = findSubstring(S, T);

function findSubstring(S, T) {
    const minLength = T.length;
    for (let letter of T) {
        console.log(letter);
    }

    return undefined;
}
